{
  "name": "@ai-skills/hold-sync-cli",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "hold-sync": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-skills/hold-sync-core": "file:../hold-sync-core",
    "@tanstack/store": "^0.8.1",
    "commander": "^13.1.0"
  }
}
